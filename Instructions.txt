---Actualizar sistema e instalar librerias del entorno---

sudo apt update
sudo apt install python3-venv python3-dev -y

----Crear el entorno virtual----
python3 -m venv venv
source venv/bin/activate

in windows: source venv/Scripts/activate

---Instalar requerimientos----

pip install -r requirements.txt
sudo apt install wmctrl scrot python3-tk -y

--- Entrenar Modelo---

yolo detect train model=yolov8n.pt data=dataset/data.yaml epochs=200 imgsz=640